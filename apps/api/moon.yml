language: 'go'
type: 'application'

tasks:
  dev:
    command: 'go run cmd/server/**'
    platform: 'system'

  build:
    command: 'go build -o server cmd/server/main.go'
    platform: 'system'
    
    deps:
      - 'proto:generate'